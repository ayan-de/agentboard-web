export interface Feature {
  icon: string
  title: string
  description: string
}

export interface ThemeInfo {
  name: string
  primary: string
  secondary: string
  accent: string
  background: string
}